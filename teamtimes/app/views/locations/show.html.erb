<p>
  Match in 
  <strong><%= @location.name %> </strong>

 on
  <strong><%= @location.date.strftime("%a, %b %d at %I:%M%p") %></strong>
</p>


<%= link_to 'Edit', edit_location_path(@location) %> |
<%= link_to 'Back', locations_path %>
